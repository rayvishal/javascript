// Create a function that takes an integer and returns true if it's divisible by 100, otherwise return false.

function divisibleby100(int) {
    console.log(int % 100 === 0 ? true : false)
}
divisibleby100(1000)